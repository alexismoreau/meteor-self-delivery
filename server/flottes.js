/**
 * Created by alexis_moreau on 30/11/2016.
 */
/*
import { Mongo } from 'meteor/mongo';
import { Random } from 'meteor/random'

const Flottes = new Mongo.Collection('flottes');

var camion = {
  id: Random.id()
};

var flotte = {
  camions: [],
  localisation: 'Paris, FR',
  dispo: true // dispo par defaut
};

camion1 = Object.create(camion);
camion2 = Object.create(camion);

flotte.camions.push(camion1, camion2);

Flottes.insert(flotte);

console.log(Flottes.find(flotte).fetch());

console.log("localisation : "+Flottes.find(flotte).fetch()[0].localisation);

let obj = Flottes.find(flotte).fetch()[0].camions;

for (var property in obj) {
    console.log("id : "+obj[property]);
}
*/

